SELECT
    ORDER_DETAIL_ID,
    ORDER_ID,
    PRODUCT_ID,
    PRODUCT_NAME,
    QUANTITY,
    UNIT_PRICE,
    LINE_TOTAL,
    DISCOUNT_PERCENT,
    CREATED_BY,
    CREATED_ON,
    UPDATED_BY,
    UPDATED_ON,
    IS_DELETED
FROM
    {{ source('HIS_SOURCES', 'ORDER_DETAIL_TABLE') }}
WHERE
    IS_DELETED = FALSE