SELECT
    ORDER_ID,
    CUSTOMER_ID,
    ORDER_DATE,
    ORDER_STATUS,
    TOTAL_AMOUNT,
    SHIPPING_ADDRESS,
    CREATED_BY,
    CREATED_ON,
    UPDATED_BY,
    UPDATED_ON,
    IS_DELETED
FROM
    {{ source('HIS_SOURCES', 'ORDER_TABLE') }}
WHERE
    IS_DELETED = FALSE